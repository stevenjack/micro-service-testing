compositionLayer:
  image: compositionlayer_compositionLayer

varnish:
  build: .
  environment:
    - VCL_CONFIG=/data/varnish/default.vcl
  links:
   - compositionLayer:'backend-host'
  ports:
    - "80"
